import './style.css';

document.addEventListener('DOMContentLoaded', () => {
  const rows = document.getElementById('dice-rows');
  if (rows) {
    rows.textContent = 'Dice visualizer loading...';
  }
});
